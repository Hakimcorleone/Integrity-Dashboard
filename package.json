{
  "name": "integrity-dashboard",
  "private": true,
  "version": "2.0.0",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "vite",
    "prepare:data": "node scripts/prepare-data.mjs",
    "prebuild": "npm run prepare:data",
    "build": "vite build",
    "preview": "vite preview",
    "test": "node --test src/lib/risk.test.js"
  },
  "dependencies": {
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^5.0.0",
    "vite": "^7.0.0"
  }
}
